# Development Configuration Profile
spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.portfolio: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG

server:
  servlet:
    session:
      cookie:
        secure: false
